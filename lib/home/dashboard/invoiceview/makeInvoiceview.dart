import 'package:flutter/material.dart';

class MakeInvoiceView extends StatelessWidget {
  const MakeInvoiceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
