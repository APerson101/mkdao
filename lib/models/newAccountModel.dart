class Account {
  String id;
  Account({
    required this.id,
  });
}

class PublicKey {
  String id;
  PublicKey({
    required this.id,
  });
}

class PrivateKey {
  String id;
  PrivateKey({
    required this.id,
  });
}
